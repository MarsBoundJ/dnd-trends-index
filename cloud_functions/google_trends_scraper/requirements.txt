# Dependencies for the CONTAINER IMAGE only — the Cloud Run job that actually
# collects. browser_trends.py is the only code this file needs to serve.
#
# The Dockerfile also pip-installs playwright, playwright-stealth and
# google-cloud-bigquery inline on the same command; they are listed here so the
# dependency set is readable without reverse-engineering the Dockerfile.
#
# Removed Sep 14, 2026: pandas, pytrends, urllib3, functions-framework,
# google-cloud-storage. Those served main.py and job.py, both deleted in the
# same change, and browser_trends.py imports none of them. For the manual
# pytrends backfill script see requirements-backfill.txt.
playwright
playwright-stealth
google-cloud-bigquery>=3.0.0
